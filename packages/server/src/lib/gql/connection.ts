import { connectionArgs, connectionDefinitions } from 'graphql-relay'

/**
 * Bidirecional pagination arguments.
 */
export { connectionArgs }

/**
 * Build connection and edge GraphQL types for pagination.
 * Learn more: https://graphql.org/learn/pagination
 * Relay specs: https://relay.dev/docs/guides/graphql-server-specification/#connections
 */
export { connectionDefinitions }
